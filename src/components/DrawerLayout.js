import React from 'react';
import {
    View,
    Image
} from 'native-base';

const DrawerLayout = (props) => {
    return (
        <View>
            <Image/>
        </View>
    );
}

export default DrawerLayout;