const initialState = {
    id: null,
    name: null,
    email: null,
    apiToken: null
};


let user;

export default user = (state = initialState, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
};